---
import Header from "../components/Header.astro"
import "../styles/global.css"
// Grab all .md files in the specific directory
const posts = await Astro.glob('../pages/posts/*.md');
// Sort them by date (Newest to Oldest)
const sortedPosts = posts.sort((a, b) => {
  return new Date(b.frontmatter.pubDate).getTime() - new Date(a.frontmatter.pubDate).getTime();
});
---
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Matt Labrecque's Blog</title>
</head>
<body>
  <Header/>
  <p>The rants and musings of a Mathematician and Computer Scientist from Northern New England. New posts every week (if I have inspiration).</p>
    
    <section>
    <h2>Recent Posts</h2>
  <ul>
  {sortedPosts.map((post) => (
    <li>
      
      <a href={post.url}>{post.frontmatter.title}</a>
    </li>
  ))}
  </ul>
  </section>
</body>

<style>
.header{text-align: center;}

</style>
